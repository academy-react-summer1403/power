/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["classapi.sepehracademy.ir" || "tahacode.ir"],
  },
  css: ["./app/global.css"],
};

export default nextConfig;
